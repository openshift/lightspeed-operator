---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: proposals.agentic.openshift.io
spec:
  group: agentic.openshift.io
  names:
    kind: Proposal
    listKind: ProposalList
    plural: proposals
    singular: proposal
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.request
      name: Request
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: "Proposal represents a unit of work managed by the agentic platform.\nIt
          is the primary resource component teams and adapters interact with.\n\nA
          Proposal defines the workflow shape inline: which steps run and which\nagent
          handles each step. Analysis is always required. Omit execution\nand/or verification
          to skip those steps.\n\nExample — analysis only (advisory):\n\n\tapiVersion:
          agentic.openshift.io/v1alpha1\n\tkind: Proposal\n\tmetadata:\n\t  name:
          one-off-investigation\n\tspec:\n\t  request: \"Investigate why pod foo is
          crashlooping\"\n\t  targetNamespaces:\n\t    - lightspeed-demo\n\t  tools:\n\t
          \   skills:\n\t      - image: registry.redhat.io/acs/acs-lightspeed-skills:latest\n\t
          \ analysis:\n\t    agent: smart\n\nExample — full remediation (analyze →
          execute → verify):\n\n\tapiVersion: agentic.openshift.io/v1alpha1\n\tkind:
          Proposal\n\tmetadata:\n\t  name: fix-nginx-cve-2024-1234\n\t  namespace:
          stackrox\n\tspec:\n\t  request: \"Fix CVE-2024-1234 in nginx:1.21\"\n\t
          \ targetNamespaces:\n\t    - lightspeed-demo\n\t  maxAttempts: 3\n\t  tools:\n\t
          \   skills:\n\t      - image: registry.redhat.io/acs/acs-lightspeed-skills:latest\n\t
          \   requiredSecrets:\n\t      - name: acs-api-token\n\t        mountAs:
          ACS_API_TOKEN\n\t  analysis:\n\t    agent: smart\n\t  execution: {}\n\t
          \ verification:\n\t    agent: fast"
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Proposal.
            properties:
              analysis:
                description: |-
                  analysis defines per-step configuration for the analysis step,
                  including which agent handles it and any per-step tools.

                  Immutable: agent and per-step tools are fixed at creation.
                properties:
                  agent:
                    description: |-
                      agent is the name of the cluster-scoped Agent CR to use for this step.
                      Defaults to "default" when omitted.
                    maxLength: 253
                    minLength: 1
                    type: string
                  tools:
                    description: |-
                      tools provides per-step tools that replace the shared spec.tools
                      for this step. Use this when different steps need different skills.
                    properties:
                      mcpServers:
                        description: |-
                          mcpServers defines external MCP (Model Context Protocol) servers the
                          agent can connect to for additional tools and context.
                        items:
                          description: "MCPServerConfig defines the configuration
                            for an MCP (Model Context Protocol)\nserver that the agent
                            can connect to for additional tools and context.\nMCP
                            servers extend the agent's capabilities beyond its built-in
                            skills.\n\nExample — connecting to an OpenShift MCP server
                            with SA token auth:\n\n\tmcpServers:\n\t  - name: openshift\n\t
                            \   url: https://mcp.openshift-lightspeed.svc:8443/sse\n\t
                            \   timeoutSeconds: 10\n\t    headers:\n\t      - name:
                            Authorization\n\t        valueFrom:\n\t          type:
                            ServiceAccountToken\n\nExample — connecting to an external
                            API with secret-based auth:\n\n\tmcpServers:\n\t  - name:
                            pagerduty\n\t    url: https://mcp-pagerduty.example.com/sse\n\t
                            \   headers:\n\t      - name: X-API-Key\n\t        valueFrom:\n\t
                            \         type: Secret\n\t          secret:\n\t            name:
                            pagerduty-api-key"
                          properties:
                            headers:
                              description: headers to send to the MCP server. Maximum
                                20 items.
                              items:
                                description: |-
                                  MCPHeader defines an HTTP header to send with every request to an
                                  MCP server. Used for authentication and routing.
                                properties:
                                  name:
                                    description: |-
                                      name of the header (e.g., "Authorization", "X-API-Key").
                                      Must be at least 1 character, containing only letters, digits, and hyphens.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                    x-kubernetes-validations:
                                    - message: name must start with a letter and contain
                                        only letters, digits, and hyphens
                                      rule: self.matches('^[A-Za-z][A-Za-z0-9-]*$')
                                  valueFrom:
                                    description: valueFrom is the source of the header
                                      value.
                                    properties:
                                      secret:
                                        description: |-
                                          secret references a Secret containing the header value.
                                          Required when type is "Secret".
                                        properties:
                                          name:
                                            description: name of the Secret.
                                            maxLength: 253
                                            minLength: 1
                                            type: string
                                        required:
                                        - name
                                        type: object
                                      type:
                                        description: |-
                                          type specifies the source type for the header value. Allowed values:
                                            - "Secret"     — reads the value from a Kubernetes Secret (use for
                                              API keys and tokens). Requires the secret field to be set.
                                            - "ServiceAccountToken" — auto-injects a Kubernetes service account token
                                              (for MCP servers that accept K8s auth).
                                            - "Client"     — the value is provided by the calling client at
                                              runtime (e.g., forwarded from a user session).
                                        enum:
                                        - Secret
                                        - ServiceAccountToken
                                        - Client
                                        type: string
                                    required:
                                    - type
                                    type: object
                                    x-kubernetes-validations:
                                    - message: secret is required when type is Secret,
                                        and forbidden otherwise
                                      rule: 'self.type == ''Secret'' ? has(self.secret)
                                        : !has(self.secret)'
                                required:
                                - name
                                - valueFrom
                                type: object
                              maxItems: 20
                              minItems: 1
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            name:
                              description: |-
                                name of the MCP server. Must start with a letter and contain only
                                lowercase alphanumeric characters and hyphens. Must be 1-253 characters.
                              maxLength: 253
                              minLength: 1
                              type: string
                              x-kubernetes-validations:
                              - message: name must start with a lowercase letter and
                                  contain only lowercase alphanumerics and hyphens
                                rule: self.matches('^[a-z][a-z0-9-]*$')
                            timeoutSeconds:
                              default: 5
                              description: |-
                                timeoutSeconds is the per-request timeout for calls to this MCP server,
                                in seconds. Default is 5.
                                Valid range: 1-300.
                              format: int32
                              maximum: 300
                              minimum: 1
                              type: integer
                            url:
                              description: |-
                                url of the MCP server (HTTP/HTTPS). Must be an HTTP or HTTPS URL,
                                maximum 2048 characters.
                              maxLength: 2048
                              minLength: 1
                              type: string
                              x-kubernetes-validations:
                              - message: url must be a valid HTTP or HTTPS URL
                                rule: isURL(self) && url(self).getScheme() in ['http',
                                  'https']
                          required:
                          - name
                          - url
                          type: object
                        maxItems: 20
                        minItems: 1
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      outputSchema:
                        description: |-
                          outputSchema is a JSON Schema object that defines additional structured
                          output fields beyond the base schema that every agent produces (diagnosis,
                          proposal, RBAC, verification plan).
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      requiredSecrets:
                        description: |-
                          requiredSecrets declares Kubernetes Secrets that the sandbox pod
                          needs at runtime. The cluster admin creates the actual Secrets
                          in the same namespace as the Proposal.
                        items:
                          description: |-
                            SecretRequirement declares a Kubernetes Secret that the sandbox needs
                            at runtime. The cluster admin creates the actual Secret in the same
                            namespace as the Proposal.
                          properties:
                            description:
                              description: |-
                                description explains what this secret is used for, helping the
                                cluster admin understand what credentials to provide.
                              maxLength: 1024
                              minLength: 1
                              type: string
                            mountAs:
                              description: |-
                                mountAs specifies how the secret is exposed in the sandbox pod.
                                Use an environment variable name (e.g., "GITHUB_TOKEN") to inject
                                as an env var, or a file path (e.g., "/etc/secrets/token") to
                                mount as a file.
                              maxLength: 512
                              minLength: 1
                              type: string
                            name:
                              description: name of the Secret (must exist in the same
                                namespace as the Proposal).
                              maxLength: 253
                              minLength: 1
                              type: string
                          required:
                          - mountAs
                          - name
                          type: object
                        maxItems: 20
                        minItems: 1
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      skills:
                        description: |-
                          skills defines one or more OCI images containing skills to mount
                          in the agent's sandbox pod. The operator creates Kubernetes image
                          volumes (requires K8s 1.34+) and mounts them into the agent's
                          skills directory. Each image must be unique within the list.
                        items:
                          description: "SkillsSource defines an OCI image containing
                            skills and optionally which\npaths within that image to
                            mount. Skills are mounted as Kubernetes image\nvolumes
                            in the agent's sandbox pod.\n\nWhen paths is omitted,
                            the entire image is mounted. When paths is specified,\nonly
                            those directories are mounted (each as a separate subPath
                            volumeMount),\nallowing selective composition of skills
                            from large shared images.\n\nExample — mount all skills
                            from a custom image:\n\n\tskills:\n\t  - image: quay.io/my-org/my-skills:latest\n\nExample
                            — selectively mount two skills from a shared image:\n\n\tskills:\n\t
                            \ - image: registry.ci.openshift.org/ocp/5.0:agentic-skills\n\t
                            \   paths:\n\t      - /skills/prometheus\n\t      - /skills/cluster-update/update-advisor"
                          properties:
                            image:
                              description: |-
                                image is the OCI image reference containing skills.
                                The operator mounts this as a Kubernetes image volume (requires K8s 1.34+).
                                Must be a valid OCI image pullspec: a domain, followed by a repository path,
                                ending with either a tag (:tag) or a digest (@algorithm:hex).
                                Must be 1-512 characters.
                              maxLength: 512
                              minLength: 1
                              type: string
                              x-kubernetes-validations:
                              - message: must start with a valid domain. valid domains
                                  must be alphanumeric characters (lowercase and uppercase)
                                  separated by the '.' character.
                                rule: self.matches('^([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])((\\.([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))+)?(:[0-9]+)?\\b')
                              - message: a valid name is required. valid names must
                                  contain lowercase alphanumeric characters separated
                                  only by the '.', '_', '__', '-' characters.
                                rule: self.find('(/[a-z0-9]+((([._]|__|[-]*)[a-z0-9]+)+)?((/[a-z0-9]+((([._]|__|[-]*)[a-z0-9]+)+)?)+)?)')
                                  != ''
                              - message: must end with a digest or a tag
                                rule: self.find('(@.*:)') != '' || self.find(':.*$')
                                  != ''
                              - message: tag must not be more than 127 characters
                                rule: 'self.find(''(@.*:)'') == '''' ? (self.find('':.*$'')
                                  != '''' ? self.find('':.*$'').substring(1).size()
                                  <= 127 : true) : true'
                              - message: tag is invalid. valid tags must begin with
                                  a word character followed by word characters, '.',
                                  or '-'
                                rule: 'self.find(''(@.*:)'') == '''' ? (self.find('':.*$'')
                                  != '''' ? self.find('':.*$'').matches('':[\\w][\\w.-]*$'')
                                  : true) : true'
                              - message: digest algorithm is not valid. valid algorithms
                                  must start with an alpha character followed by alphanumeric
                                  characters and may contain '-', '_', '+', and '.'
                                  characters.
                                rule: 'self.find(''(@.*:)'') != '''' ? self.find(''(@.*:)'').matches(''(@[A-Za-z][A-Za-z0-9]*([_+.][A-Za-z][A-Za-z0-9]*)*[:])'')
                                  : true'
                              - message: digest must be at least 32 characters
                                rule: 'self.find(''(@.*:)'') != '''' ? self.find('':.*$'').substring(1).size()
                                  >= 32 : true'
                              - message: digest must only contain hex characters (A-F,
                                  a-f, 0-9)
                                rule: 'self.find(''(@.*:)'') != '''' ? self.find('':.*$'').matches('':[0-9A-Fa-f]*$'')
                                  : true'
                            paths:
                              description: |-
                                paths restricts which directories from the image are mounted.
                                Each path is mounted as a separate subPath volumeMount into the agent's
                                skills directory. The last segment of each path becomes the mount name
                                (e.g., "/skills/prometheus" mounts as "prometheus").

                                Each path must be an absolute file path: starts with "/", no ".."
                                or "." segments, no double slashes, no trailing slash, and only
                                alphanumeric characters, hyphens, underscores, dots, and slashes.

                                When omitted, the entire image is mounted as a single volume.
                                Maximum 50 items.
                              items:
                                maxLength: 512
                                minLength: 2
                                type: string
                              maxItems: 50
                              minItems: 1
                              type: array
                              x-kubernetes-list-type: atomic
                              x-kubernetes-validations:
                              - message: each path must be 2-512 characters
                                rule: self.all(p, p.size() >= 2 && p.size() <= 512)
                              - message: each path must be absolute (start with '/')
                                rule: self.all(p, p.startsWith('/'))
                              - message: paths must not end with '/'
                                rule: self.all(p, !p.endsWith('/'))
                              - message: paths must not contain double slashes
                                rule: self.all(p, !p.contains('//'))
                              - message: paths must not contain '.' or '..' segments
                                rule: self.all(p, !p.contains('/../') && !p.endsWith('/..')
                                  && !p.contains('/./') && !p.endsWith('/.'))
                              - message: paths may only contain alphanumeric characters,
                                  '/', '_', '.', and '-'
                                rule: self.all(p, p.matches('^[a-zA-Z0-9/_.-]+$'))
                          required:
                          - image
                          type: object
                        maxItems: 20
                        minItems: 1
                        type: array
                        x-kubernetes-list-map-keys:
                        - image
                        x-kubernetes-list-type: map
                    type: object
                type: object
                x-kubernetes-validations:
                - message: analysis is immutable after creation
                  rule: self == oldSelf
              execution:
                description: |-
                  execution defines per-step configuration for the execution step.
                  Omit to skip execution (advisory/assisted patterns).

                  Immutable: agent and per-step tools are fixed at creation.
                properties:
                  agent:
                    description: |-
                      agent is the name of the cluster-scoped Agent CR to use for this step.
                      Defaults to "default" when omitted.
                    maxLength: 253
                    minLength: 1
                    type: string
                  tools:
                    description: |-
                      tools provides per-step tools that replace the shared spec.tools
                      for this step. Use this when different steps need different skills.
                    properties:
                      mcpServers:
                        description: |-
                          mcpServers defines external MCP (Model Context Protocol) servers the
                          agent can connect to for additional tools and context.
                        items:
                          description: "MCPServerConfig defines the configuration
                            for an MCP (Model Context Protocol)\nserver that the agent
                            can connect to for additional tools and context.\nMCP
                            servers extend the agent's capabilities beyond its built-in
                            skills.\n\nExample — connecting to an OpenShift MCP server
                            with SA token auth:\n\n\tmcpServers:\n\t  - name: openshift\n\t
                            \   url: https://mcp.openshift-lightspeed.svc:8443/sse\n\t
                            \   timeoutSeconds: 10\n\t    headers:\n\t      - name:
                            Authorization\n\t        valueFrom:\n\t          type:
                            ServiceAccountToken\n\nExample — connecting to an external
                            API with secret-based auth:\n\n\tmcpServers:\n\t  - name:
                            pagerduty\n\t    url: https://mcp-pagerduty.example.com/sse\n\t
                            \   headers:\n\t      - name: X-API-Key\n\t        valueFrom:\n\t
                            \         type: Secret\n\t          secret:\n\t            name:
                            pagerduty-api-key"
                          properties:
                            headers:
                              description: headers to send to the MCP server. Maximum
                                20 items.
                              items:
                                description: |-
                                  MCPHeader defines an HTTP header to send with every request to an
                                  MCP server. Used for authentication and routing.
                                properties:
                                  name:
                                    description: |-
                                      name of the header (e.g., "Authorization", "X-API-Key").
                                      Must be at least 1 character, containing only letters, digits, and hyphens.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                    x-kubernetes-validations:
                                    - message: name must start with a letter and contain
                                        only letters, digits, and hyphens
                                      rule: self.matches('^[A-Za-z][A-Za-z0-9-]*$')
                                  valueFrom:
                                    description: valueFrom is the source of the header
                                      value.
                                    properties:
                                      secret:
                                        description: |-
                                          secret references a Secret containing the header value.
                                          Required when type is "Secret".
                                        properties:
                                          name:
                                            description: name of the Secret.
                                            maxLength: 253
                                            minLength: 1
                                            type: string
                                        required:
                                        - name
                                        type: object
                                      type:
                                        description: |-
                                          type specifies the source type for the header value. Allowed values:
                                            - "Secret"     — reads the value from a Kubernetes Secret (use for
                                              API keys and tokens). Requires the secret field to be set.
                                            - "ServiceAccountToken" — auto-injects a Kubernetes service account token
                                              (for MCP servers that accept K8s auth).
                                            - "Client"     — the value is provided by the calling client at
                                              runtime (e.g., forwarded from a user session).
                                        enum:
                                        - Secret
                                        - ServiceAccountToken
                                        - Client
                                        type: string
                                    required:
                                    - type
                                    type: object
                                    x-kubernetes-validations:
                                    - message: secret is required when type is Secret,
                                        and forbidden otherwise
                                      rule: 'self.type == ''Secret'' ? has(self.secret)
                                        : !has(self.secret)'
                                required:
                                - name
                                - valueFrom
                                type: object
                              maxItems: 20
                              minItems: 1
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            name:
                              description: |-
                                name of the MCP server. Must start with a letter and contain only
                                lowercase alphanumeric characters and hyphens. Must be 1-253 characters.
                              maxLength: 253
                              minLength: 1
                              type: string
                              x-kubernetes-validations:
                              - message: name must start with a lowercase letter and
                                  contain only lowercase alphanumerics and hyphens
                                rule: self.matches('^[a-z][a-z0-9-]*$')
                            timeoutSeconds:
                              default: 5
                              description: |-
                                timeoutSeconds is the per-request timeout for calls to this MCP server,
                                in seconds. Default is 5.
                                Valid range: 1-300.
                              format: int32
                              maximum: 300
                              minimum: 1
                              type: integer
                            url:
                              description: |-
                                url of the MCP server (HTTP/HTTPS). Must be an HTTP or HTTPS URL,
                                maximum 2048 characters.
                              maxLength: 2048
                              minLength: 1
                              type: string
                              x-kubernetes-validations:
                              - message: url must be a valid HTTP or HTTPS URL
                                rule: isURL(self) && url(self).getScheme() in ['http',
                                  'https']
                          required:
                          - name
                          - url
                          type: object
                        maxItems: 20
                        minItems: 1
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      outputSchema:
                        description: |-
                          outputSchema is a JSON Schema object that defines additional structured
                          output fields beyond the base schema that every agent produces (diagnosis,
                          proposal, RBAC, verification plan).
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      requiredSecrets:
                        description: |-
                          requiredSecrets declares Kubernetes Secrets that the sandbox pod
                          needs at runtime. The cluster admin creates the actual Secrets
                          in the same namespace as the Proposal.
                        items:
                          description: |-
                            SecretRequirement declares a Kubernetes Secret that the sandbox needs
                            at runtime. The cluster admin creates the actual Secret in the same
                            namespace as the Proposal.
                          properties:
                            description:
                              description: |-
                                description explains what this secret is used for, helping the
                                cluster admin understand what credentials to provide.
                              maxLength: 1024
                              minLength: 1
                              type: string
                            mountAs:
                              description: |-
                                mountAs specifies how the secret is exposed in the sandbox pod.
                                Use an environment variable name (e.g., "GITHUB_TOKEN") to inject
                                as an env var, or a file path (e.g., "/etc/secrets/token") to
                                mount as a file.
                              maxLength: 512
                              minLength: 1
                              type: string
                            name:
                              description: name of the Secret (must exist in the same
                                namespace as the Proposal).
                              maxLength: 253
                              minLength: 1
                              type: string
                          required:
                          - mountAs
                          - name
                          type: object
                        maxItems: 20
                        minItems: 1
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      skills:
                        description: |-
                          skills defines one or more OCI images containing skills to mount
                          in the agent's sandbox pod. The operator creates Kubernetes image
                          volumes (requires K8s 1.34+) and mounts them into the agent's
                          skills directory. Each image must be unique within the list.
                        items:
                          description: "SkillsSource defines an OCI image containing
                            skills and optionally which\npaths within that image to
                            mount. Skills are mounted as Kubernetes image\nvolumes
                            in the agent's sandbox pod.\n\nWhen paths is omitted,
                            the entire image is mounted. When paths is specified,\nonly
                            those directories are mounted (each as a separate subPath
                            volumeMount),\nallowing selective composition of skills
                            from large shared images.\n\nExample — mount all skills
                            from a custom image:\n\n\tskills:\n\t  - image: quay.io/my-org/my-skills:latest\n\nExample
                            — selectively mount two skills from a shared image:\n\n\tskills:\n\t
                            \ - image: registry.ci.openshift.org/ocp/5.0:agentic-skills\n\t
                            \   paths:\n\t      - /skills/prometheus\n\t      - /skills/cluster-update/update-advisor"
                          properties:
                            image:
                              description: |-
                                image is the OCI image reference containing skills.
                                The operator mounts this as a Kubernetes image volume (requires K8s 1.34+).
                                Must be a valid OCI image pullspec: a domain, followed by a repository path,
                                ending with either a tag (:tag) or a digest (@algorithm:hex).
                                Must be 1-512 characters.
                              maxLength: 512
                              minLength: 1
                              type: string
                              x-kubernetes-validations:
                              - message: must start with a valid domain. valid domains
                                  must be alphanumeric characters (lowercase and uppercase)
                                  separated by the '.' character.
                                rule: self.matches('^([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])((\\.([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))+)?(:[0-9]+)?\\b')
                              - message: a valid name is required. valid names must
                                  contain lowercase alphanumeric characters separated
                                  only by the '.', '_', '__', '-' characters.
                                rule: self.find('(/[a-z0-9]+((([._]|__|[-]*)[a-z0-9]+)+)?((/[a-z0-9]+((([._]|__|[-]*)[a-z0-9]+)+)?)+)?)')
                                  != ''
                              - message: must end with a digest or a tag
                                rule: self.find('(@.*:)') != '' || self.find(':.*$')
                                  != ''
                              - message: tag must not be more than 127 characters
                                rule: 'self.find(''(@.*:)'') == '''' ? (self.find('':.*$'')
                                  != '''' ? self.find('':.*$'').substring(1).size()
                                  <= 127 : true) : true'
                              - message: tag is invalid. valid tags must begin with
                                  a word character followed by word characters, '.',
                                  or '-'
                                rule: 'self.find(''(@.*:)'') == '''' ? (self.find('':.*$'')
                                  != '''' ? self.find('':.*$'').matches('':[\\w][\\w.-]*$'')
                                  : true) : true'
                              - message: digest algorithm is not valid. valid algorithms
                                  must start with an alpha character followed by alphanumeric
                                  characters and may contain '-', '_', '+', and '.'
                                  characters.
                                rule: 'self.find(''(@.*:)'') != '''' ? self.find(''(@.*:)'').matches(''(@[A-Za-z][A-Za-z0-9]*([_+.][A-Za-z][A-Za-z0-9]*)*[:])'')
                                  : true'
                              - message: digest must be at least 32 characters
                                rule: 'self.find(''(@.*:)'') != '''' ? self.find('':.*$'').substring(1).size()
                                  >= 32 : true'
                              - message: digest must only contain hex characters (A-F,
                                  a-f, 0-9)
                                rule: 'self.find(''(@.*:)'') != '''' ? self.find('':.*$'').matches('':[0-9A-Fa-f]*$'')
                                  : true'
                            paths:
                              description: |-
                                paths restricts which directories from the image are mounted.
                                Each path is mounted as a separate subPath volumeMount into the agent's
                                skills directory. The last segment of each path becomes the mount name
                                (e.g., "/skills/prometheus" mounts as "prometheus").

                                Each path must be an absolute file path: starts with "/", no ".."
                                or "." segments, no double slashes, no trailing slash, and only
                                alphanumeric characters, hyphens, underscores, dots, and slashes.

                                When omitted, the entire image is mounted as a single volume.
                                Maximum 50 items.
                              items:
                                maxLength: 512
                                minLength: 2
                                type: string
                              maxItems: 50
                              minItems: 1
                              type: array
                              x-kubernetes-list-type: atomic
                              x-kubernetes-validations:
                              - message: each path must be 2-512 characters
                                rule: self.all(p, p.size() >= 2 && p.size() <= 512)
                              - message: each path must be absolute (start with '/')
                                rule: self.all(p, p.startsWith('/'))
                              - message: paths must not end with '/'
                                rule: self.all(p, !p.endsWith('/'))
                              - message: paths must not contain double slashes
                                rule: self.all(p, !p.contains('//'))
                              - message: paths must not contain '.' or '..' segments
                                rule: self.all(p, !p.contains('/../') && !p.endsWith('/..')
                                  && !p.contains('/./') && !p.endsWith('/.'))
                              - message: paths may only contain alphanumeric characters,
                                  '/', '_', '.', and '-'
                                rule: self.all(p, p.matches('^[a-zA-Z0-9/_.-]+$'))
                          required:
                          - image
                          type: object
                        maxItems: 20
                        minItems: 1
                        type: array
                        x-kubernetes-list-map-keys:
                        - image
                        x-kubernetes-list-type: map
                    type: object
                type: object
                x-kubernetes-validations:
                - message: execution is immutable after creation
                  rule: self == oldSelf
              maxAttempts:
                description: |-
                  maxAttempts sets the maximum number of retry attempts for this proposal.

                  Mutable: the console UI patches this at approval time so the user
                  can set a custom retry limit before execution begins.
                format: int32
                maximum: 20
                minimum: 0
                type: integer
              parent:
                description: |-
                  parent references the parent proposal in an escalation chain.
                  Set at creation by the operator (auto-escalation) or console
                  (manual escalation). Component teams should not set this field.

                  Immutable: the escalation lineage cannot change after creation.
                properties:
                  name:
                    description: name of the parent Proposal.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: parent is immutable after creation
                  rule: self == oldSelf
              request:
                description: |-
                  request is the user's original request, alert description, or a
                  description of what triggered this proposal. This text is passed to
                  the analysis agent as the primary input.

                  Immutable: Proposals are run-to-completion (like Jobs). To change
                  the request, create a new Proposal. Use spec.revision for iterative
                  feedback on an existing analysis.
                maxLength: 32768
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: request is immutable after creation
                  rule: self == oldSelf
              revision:
                description: |-
                  revision is incremented by the user (or console UI) each time they
                  submit revision feedback for the analysis.

                  Mutable: this is the designated mutation point for iterative
                  feedback. Incrementing revision triggers re-analysis with the
                  user's revision context appended to the original request.
                format: int32
                minimum: 0
                type: integer
              revisionFeedback:
                description: |-
                  revisionFeedback is the user's free-text feedback that accompanies a
                  revision request. When the user increments spec.revision, they set
                  this field to describe what they want changed about the analysis.
                  The operator includes this text in the revision context sent to the
                  analysis agent.

                  Mutable: updated alongside spec.revision to provide revision context.
                maxLength: 32768
                type: string
              targetNamespaces:
                description: |-
                  targetNamespaces are the Kubernetes namespace(s) this proposal
                  operates on. Used for RBAC scoping and context to the analysis agent.

                  Immutable: RBAC scoping is fixed at creation. Changing target
                  namespaces mid-flight would invalidate the analysis and any
                  granted execution RBAC.
                items:
                  maxLength: 63
                  minLength: 1
                  type: string
                maxItems: 50
                minItems: 1
                type: array
                x-kubernetes-list-type: atomic
                x-kubernetes-validations:
                - message: each namespace must be a valid DNS label
                  rule: self.all(ns, !format.dns1123Label().validate(ns).hasValue())
                - message: targetNamespaces is immutable after creation
                  rule: self == oldSelf
              tools:
                description: |-
                  tools defines the default tools for all steps: skills images,
                  required secrets, and output schema. Per-step tools
                  (analysis.tools, execution.tools, verification.tools) replace
                  this default for individual steps.

                  Immutable: the skills and secrets available to the agent are
                  fixed at creation. Changing tools mid-flight could violate the
                  assumptions of an in-progress analysis or execution.
                properties:
                  mcpServers:
                    description: |-
                      mcpServers defines external MCP (Model Context Protocol) servers the
                      agent can connect to for additional tools and context.
                    items:
                      description: "MCPServerConfig defines the configuration for
                        an MCP (Model Context Protocol)\nserver that the agent can
                        connect to for additional tools and context.\nMCP servers
                        extend the agent's capabilities beyond its built-in skills.\n\nExample
                        — connecting to an OpenShift MCP server with SA token auth:\n\n\tmcpServers:\n\t
                        \ - name: openshift\n\t    url: https://mcp.openshift-lightspeed.svc:8443/sse\n\t
                        \   timeoutSeconds: 10\n\t    headers:\n\t      - name: Authorization\n\t
                        \       valueFrom:\n\t          type: ServiceAccountToken\n\nExample
                        — connecting to an external API with secret-based auth:\n\n\tmcpServers:\n\t
                        \ - name: pagerduty\n\t    url: https://mcp-pagerduty.example.com/sse\n\t
                        \   headers:\n\t      - name: X-API-Key\n\t        valueFrom:\n\t
                        \         type: Secret\n\t          secret:\n\t            name:
                        pagerduty-api-key"
                      properties:
                        headers:
                          description: headers to send to the MCP server. Maximum
                            20 items.
                          items:
                            description: |-
                              MCPHeader defines an HTTP header to send with every request to an
                              MCP server. Used for authentication and routing.
                            properties:
                              name:
                                description: |-
                                  name of the header (e.g., "Authorization", "X-API-Key").
                                  Must be at least 1 character, containing only letters, digits, and hyphens.
                                maxLength: 253
                                minLength: 1
                                type: string
                                x-kubernetes-validations:
                                - message: name must start with a letter and contain
                                    only letters, digits, and hyphens
                                  rule: self.matches('^[A-Za-z][A-Za-z0-9-]*$')
                              valueFrom:
                                description: valueFrom is the source of the header
                                  value.
                                properties:
                                  secret:
                                    description: |-
                                      secret references a Secret containing the header value.
                                      Required when type is "Secret".
                                    properties:
                                      name:
                                        description: name of the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        type: string
                                    required:
                                    - name
                                    type: object
                                  type:
                                    description: |-
                                      type specifies the source type for the header value. Allowed values:
                                        - "Secret"     — reads the value from a Kubernetes Secret (use for
                                          API keys and tokens). Requires the secret field to be set.
                                        - "ServiceAccountToken" — auto-injects a Kubernetes service account token
                                          (for MCP servers that accept K8s auth).
                                        - "Client"     — the value is provided by the calling client at
                                          runtime (e.g., forwarded from a user session).
                                    enum:
                                    - Secret
                                    - ServiceAccountToken
                                    - Client
                                    type: string
                                required:
                                - type
                                type: object
                                x-kubernetes-validations:
                                - message: secret is required when type is Secret,
                                    and forbidden otherwise
                                  rule: 'self.type == ''Secret'' ? has(self.secret)
                                    : !has(self.secret)'
                            required:
                            - name
                            - valueFrom
                            type: object
                          maxItems: 20
                          minItems: 1
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        name:
                          description: |-
                            name of the MCP server. Must start with a letter and contain only
                            lowercase alphanumeric characters and hyphens. Must be 1-253 characters.
                          maxLength: 253
                          minLength: 1
                          type: string
                          x-kubernetes-validations:
                          - message: name must start with a lowercase letter and contain
                              only lowercase alphanumerics and hyphens
                            rule: self.matches('^[a-z][a-z0-9-]*$')
                        timeoutSeconds:
                          default: 5
                          description: |-
                            timeoutSeconds is the per-request timeout for calls to this MCP server,
                            in seconds. Default is 5.
                            Valid range: 1-300.
                          format: int32
                          maximum: 300
                          minimum: 1
                          type: integer
                        url:
                          description: |-
                            url of the MCP server (HTTP/HTTPS). Must be an HTTP or HTTPS URL,
                            maximum 2048 characters.
                          maxLength: 2048
                          minLength: 1
                          type: string
                          x-kubernetes-validations:
                          - message: url must be a valid HTTP or HTTPS URL
                            rule: isURL(self) && url(self).getScheme() in ['http',
                              'https']
                      required:
                      - name
                      - url
                      type: object
                    maxItems: 20
                    minItems: 1
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  outputSchema:
                    description: |-
                      outputSchema is a JSON Schema object that defines additional structured
                      output fields beyond the base schema that every agent produces (diagnosis,
                      proposal, RBAC, verification plan).
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  requiredSecrets:
                    description: |-
                      requiredSecrets declares Kubernetes Secrets that the sandbox pod
                      needs at runtime. The cluster admin creates the actual Secrets
                      in the same namespace as the Proposal.
                    items:
                      description: |-
                        SecretRequirement declares a Kubernetes Secret that the sandbox needs
                        at runtime. The cluster admin creates the actual Secret in the same
                        namespace as the Proposal.
                      properties:
                        description:
                          description: |-
                            description explains what this secret is used for, helping the
                            cluster admin understand what credentials to provide.
                          maxLength: 1024
                          minLength: 1
                          type: string
                        mountAs:
                          description: |-
                            mountAs specifies how the secret is exposed in the sandbox pod.
                            Use an environment variable name (e.g., "GITHUB_TOKEN") to inject
                            as an env var, or a file path (e.g., "/etc/secrets/token") to
                            mount as a file.
                          maxLength: 512
                          minLength: 1
                          type: string
                        name:
                          description: name of the Secret (must exist in the same
                            namespace as the Proposal).
                          maxLength: 253
                          minLength: 1
                          type: string
                      required:
                      - mountAs
                      - name
                      type: object
                    maxItems: 20
                    minItems: 1
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  skills:
                    description: |-
                      skills defines one or more OCI images containing skills to mount
                      in the agent's sandbox pod. The operator creates Kubernetes image
                      volumes (requires K8s 1.34+) and mounts them into the agent's
                      skills directory. Each image must be unique within the list.
                    items:
                      description: "SkillsSource defines an OCI image containing skills
                        and optionally which\npaths within that image to mount. Skills
                        are mounted as Kubernetes image\nvolumes in the agent's sandbox
                        pod.\n\nWhen paths is omitted, the entire image is mounted.
                        When paths is specified,\nonly those directories are mounted
                        (each as a separate subPath volumeMount),\nallowing selective
                        composition of skills from large shared images.\n\nExample
                        — mount all skills from a custom image:\n\n\tskills:\n\t  -
                        image: quay.io/my-org/my-skills:latest\n\nExample — selectively
                        mount two skills from a shared image:\n\n\tskills:\n\t  -
                        image: registry.ci.openshift.org/ocp/5.0:agentic-skills\n\t
                        \   paths:\n\t      - /skills/prometheus\n\t      - /skills/cluster-update/update-advisor"
                      properties:
                        image:
                          description: |-
                            image is the OCI image reference containing skills.
                            The operator mounts this as a Kubernetes image volume (requires K8s 1.34+).
                            Must be a valid OCI image pullspec: a domain, followed by a repository path,
                            ending with either a tag (:tag) or a digest (@algorithm:hex).
                            Must be 1-512 characters.
                          maxLength: 512
                          minLength: 1
                          type: string
                          x-kubernetes-validations:
                          - message: must start with a valid domain. valid domains
                              must be alphanumeric characters (lowercase and uppercase)
                              separated by the '.' character.
                            rule: self.matches('^([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])((\\.([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))+)?(:[0-9]+)?\\b')
                          - message: a valid name is required. valid names must contain
                              lowercase alphanumeric characters separated only by
                              the '.', '_', '__', '-' characters.
                            rule: self.find('(/[a-z0-9]+((([._]|__|[-]*)[a-z0-9]+)+)?((/[a-z0-9]+((([._]|__|[-]*)[a-z0-9]+)+)?)+)?)')
                              != ''
                          - message: must end with a digest or a tag
                            rule: self.find('(@.*:)') != '' || self.find(':.*$') !=
                              ''
                          - message: tag must not be more than 127 characters
                            rule: 'self.find(''(@.*:)'') == '''' ? (self.find('':.*$'')
                              != '''' ? self.find('':.*$'').substring(1).size() <=
                              127 : true) : true'
                          - message: tag is invalid. valid tags must begin with a
                              word character followed by word characters, '.', or
                              '-'
                            rule: 'self.find(''(@.*:)'') == '''' ? (self.find('':.*$'')
                              != '''' ? self.find('':.*$'').matches('':[\\w][\\w.-]*$'')
                              : true) : true'
                          - message: digest algorithm is not valid. valid algorithms
                              must start with an alpha character followed by alphanumeric
                              characters and may contain '-', '_', '+', and '.' characters.
                            rule: 'self.find(''(@.*:)'') != '''' ? self.find(''(@.*:)'').matches(''(@[A-Za-z][A-Za-z0-9]*([_+.][A-Za-z][A-Za-z0-9]*)*[:])'')
                              : true'
                          - message: digest must be at least 32 characters
                            rule: 'self.find(''(@.*:)'') != '''' ? self.find('':.*$'').substring(1).size()
                              >= 32 : true'
                          - message: digest must only contain hex characters (A-F,
                              a-f, 0-9)
                            rule: 'self.find(''(@.*:)'') != '''' ? self.find('':.*$'').matches('':[0-9A-Fa-f]*$'')
                              : true'
                        paths:
                          description: |-
                            paths restricts which directories from the image are mounted.
                            Each path is mounted as a separate subPath volumeMount into the agent's
                            skills directory. The last segment of each path becomes the mount name
                            (e.g., "/skills/prometheus" mounts as "prometheus").

                            Each path must be an absolute file path: starts with "/", no ".."
                            or "." segments, no double slashes, no trailing slash, and only
                            alphanumeric characters, hyphens, underscores, dots, and slashes.

                            When omitted, the entire image is mounted as a single volume.
                            Maximum 50 items.
                          items:
                            maxLength: 512
                            minLength: 2
                            type: string
                          maxItems: 50
                          minItems: 1
                          type: array
                          x-kubernetes-list-type: atomic
                          x-kubernetes-validations:
                          - message: each path must be 2-512 characters
                            rule: self.all(p, p.size() >= 2 && p.size() <= 512)
                          - message: each path must be absolute (start with '/')
                            rule: self.all(p, p.startsWith('/'))
                          - message: paths must not end with '/'
                            rule: self.all(p, !p.endsWith('/'))
                          - message: paths must not contain double slashes
                            rule: self.all(p, !p.contains('//'))
                          - message: paths must not contain '.' or '..' segments
                            rule: self.all(p, !p.contains('/../') && !p.endsWith('/..')
                              && !p.contains('/./') && !p.endsWith('/.'))
                          - message: paths may only contain alphanumeric characters,
                              '/', '_', '.', and '-'
                            rule: self.all(p, p.matches('^[a-zA-Z0-9/_.-]+$'))
                      required:
                      - image
                      type: object
                    maxItems: 20
                    minItems: 1
                    type: array
                    x-kubernetes-list-map-keys:
                    - image
                    x-kubernetes-list-type: map
                type: object
                x-kubernetes-validations:
                - message: tools is immutable after creation
                  rule: self == oldSelf
              verification:
                description: |-
                  verification defines per-step configuration for the verification step.
                  Omit to skip verification.

                  Immutable: agent and per-step tools are fixed at creation.
                properties:
                  agent:
                    description: |-
                      agent is the name of the cluster-scoped Agent CR to use for this step.
                      Defaults to "default" when omitted.
                    maxLength: 253
                    minLength: 1
                    type: string
                  tools:
                    description: |-
                      tools provides per-step tools that replace the shared spec.tools
                      for this step. Use this when different steps need different skills.
                    properties:
                      mcpServers:
                        description: |-
                          mcpServers defines external MCP (Model Context Protocol) servers the
                          agent can connect to for additional tools and context.
                        items:
                          description: "MCPServerConfig defines the configuration
                            for an MCP (Model Context Protocol)\nserver that the agent
                            can connect to for additional tools and context.\nMCP
                            servers extend the agent's capabilities beyond its built-in
                            skills.\n\nExample — connecting to an OpenShift MCP server
                            with SA token auth:\n\n\tmcpServers:\n\t  - name: openshift\n\t
                            \   url: https://mcp.openshift-lightspeed.svc:8443/sse\n\t
                            \   timeoutSeconds: 10\n\t    headers:\n\t      - name:
                            Authorization\n\t        valueFrom:\n\t          type:
                            ServiceAccountToken\n\nExample — connecting to an external
                            API with secret-based auth:\n\n\tmcpServers:\n\t  - name:
                            pagerduty\n\t    url: https://mcp-pagerduty.example.com/sse\n\t
                            \   headers:\n\t      - name: X-API-Key\n\t        valueFrom:\n\t
                            \         type: Secret\n\t          secret:\n\t            name:
                            pagerduty-api-key"
                          properties:
                            headers:
                              description: headers to send to the MCP server. Maximum
                                20 items.
                              items:
                                description: |-
                                  MCPHeader defines an HTTP header to send with every request to an
                                  MCP server. Used for authentication and routing.
                                properties:
                                  name:
                                    description: |-
                                      name of the header (e.g., "Authorization", "X-API-Key").
                                      Must be at least 1 character, containing only letters, digits, and hyphens.
                                    maxLength: 253
                                    minLength: 1
                                    type: string
                                    x-kubernetes-validations:
                                    - message: name must start with a letter and contain
                                        only letters, digits, and hyphens
                                      rule: self.matches('^[A-Za-z][A-Za-z0-9-]*$')
                                  valueFrom:
                                    description: valueFrom is the source of the header
                                      value.
                                    properties:
                                      secret:
                                        description: |-
                                          secret references a Secret containing the header value.
                                          Required when type is "Secret".
                                        properties:
                                          name:
                                            description: name of the Secret.
                                            maxLength: 253
                                            minLength: 1
                                            type: string
                                        required:
                                        - name
                                        type: object
                                      type:
                                        description: |-
                                          type specifies the source type for the header value. Allowed values:
                                            - "Secret"     — reads the value from a Kubernetes Secret (use for
                                              API keys and tokens). Requires the secret field to be set.
                                            - "ServiceAccountToken" — auto-injects a Kubernetes service account token
                                              (for MCP servers that accept K8s auth).
                                            - "Client"     — the value is provided by the calling client at
                                              runtime (e.g., forwarded from a user session).
                                        enum:
                                        - Secret
                                        - ServiceAccountToken
                                        - Client
                                        type: string
                                    required:
                                    - type
                                    type: object
                                    x-kubernetes-validations:
                                    - message: secret is required when type is Secret,
                                        and forbidden otherwise
                                      rule: 'self.type == ''Secret'' ? has(self.secret)
                                        : !has(self.secret)'
                                required:
                                - name
                                - valueFrom
                                type: object
                              maxItems: 20
                              minItems: 1
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            name:
                              description: |-
                                name of the MCP server. Must start with a letter and contain only
                                lowercase alphanumeric characters and hyphens. Must be 1-253 characters.
                              maxLength: 253
                              minLength: 1
                              type: string
                              x-kubernetes-validations:
                              - message: name must start with a lowercase letter and
                                  contain only lowercase alphanumerics and hyphens
                                rule: self.matches('^[a-z][a-z0-9-]*$')
                            timeoutSeconds:
                              default: 5
                              description: |-
                                timeoutSeconds is the per-request timeout for calls to this MCP server,
                                in seconds. Default is 5.
                                Valid range: 1-300.
                              format: int32
                              maximum: 300
                              minimum: 1
                              type: integer
                            url:
                              description: |-
                                url of the MCP server (HTTP/HTTPS). Must be an HTTP or HTTPS URL,
                                maximum 2048 characters.
                              maxLength: 2048
                              minLength: 1
                              type: string
                              x-kubernetes-validations:
                              - message: url must be a valid HTTP or HTTPS URL
                                rule: isURL(self) && url(self).getScheme() in ['http',
                                  'https']
                          required:
                          - name
                          - url
                          type: object
                        maxItems: 20
                        minItems: 1
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      outputSchema:
                        description: |-
                          outputSchema is a JSON Schema object that defines additional structured
                          output fields beyond the base schema that every agent produces (diagnosis,
                          proposal, RBAC, verification plan).
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      requiredSecrets:
                        description: |-
                          requiredSecrets declares Kubernetes Secrets that the sandbox pod
                          needs at runtime. The cluster admin creates the actual Secrets
                          in the same namespace as the Proposal.
                        items:
                          description: |-
                            SecretRequirement declares a Kubernetes Secret that the sandbox needs
                            at runtime. The cluster admin creates the actual Secret in the same
                            namespace as the Proposal.
                          properties:
                            description:
                              description: |-
                                description explains what this secret is used for, helping the
                                cluster admin understand what credentials to provide.
                              maxLength: 1024
                              minLength: 1
                              type: string
                            mountAs:
                              description: |-
                                mountAs specifies how the secret is exposed in the sandbox pod.
                                Use an environment variable name (e.g., "GITHUB_TOKEN") to inject
                                as an env var, or a file path (e.g., "/etc/secrets/token") to
                                mount as a file.
                              maxLength: 512
                              minLength: 1
                              type: string
                            name:
                              description: name of the Secret (must exist in the same
                                namespace as the Proposal).
                              maxLength: 253
                              minLength: 1
                              type: string
                          required:
                          - mountAs
                          - name
                          type: object
                        maxItems: 20
                        minItems: 1
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      skills:
                        description: |-
                          skills defines one or more OCI images containing skills to mount
                          in the agent's sandbox pod. The operator creates Kubernetes image
                          volumes (requires K8s 1.34+) and mounts them into the agent's
                          skills directory. Each image must be unique within the list.
                        items:
                          description: "SkillsSource defines an OCI image containing
                            skills and optionally which\npaths within that image to
                            mount. Skills are mounted as Kubernetes image\nvolumes
                            in the agent's sandbox pod.\n\nWhen paths is omitted,
                            the entire image is mounted. When paths is specified,\nonly
                            those directories are mounted (each as a separate subPath
                            volumeMount),\nallowing selective composition of skills
                            from large shared images.\n\nExample — mount all skills
                            from a custom image:\n\n\tskills:\n\t  - image: quay.io/my-org/my-skills:latest\n\nExample
                            — selectively mount two skills from a shared image:\n\n\tskills:\n\t
                            \ - image: registry.ci.openshift.org/ocp/5.0:agentic-skills\n\t
                            \   paths:\n\t      - /skills/prometheus\n\t      - /skills/cluster-update/update-advisor"
                          properties:
                            image:
                              description: |-
                                image is the OCI image reference containing skills.
                                The operator mounts this as a Kubernetes image volume (requires K8s 1.34+).
                                Must be a valid OCI image pullspec: a domain, followed by a repository path,
                                ending with either a tag (:tag) or a digest (@algorithm:hex).
                                Must be 1-512 characters.
                              maxLength: 512
                              minLength: 1
                              type: string
                              x-kubernetes-validations:
                              - message: must start with a valid domain. valid domains
                                  must be alphanumeric characters (lowercase and uppercase)
                                  separated by the '.' character.
                                rule: self.matches('^([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])((\\.([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]))+)?(:[0-9]+)?\\b')
                              - message: a valid name is required. valid names must
                                  contain lowercase alphanumeric characters separated
                                  only by the '.', '_', '__', '-' characters.
                                rule: self.find('(/[a-z0-9]+((([._]|__|[-]*)[a-z0-9]+)+)?((/[a-z0-9]+((([._]|__|[-]*)[a-z0-9]+)+)?)+)?)')
                                  != ''
                              - message: must end with a digest or a tag
                                rule: self.find('(@.*:)') != '' || self.find(':.*$')
                                  != ''
                              - message: tag must not be more than 127 characters
                                rule: 'self.find(''(@.*:)'') == '''' ? (self.find('':.*$'')
                                  != '''' ? self.find('':.*$'').substring(1).size()
                                  <= 127 : true) : true'
                              - message: tag is invalid. valid tags must begin with
                                  a word character followed by word characters, '.',
                                  or '-'
                                rule: 'self.find(''(@.*:)'') == '''' ? (self.find('':.*$'')
                                  != '''' ? self.find('':.*$'').matches('':[\\w][\\w.-]*$'')
                                  : true) : true'
                              - message: digest algorithm is not valid. valid algorithms
                                  must start with an alpha character followed by alphanumeric
                                  characters and may contain '-', '_', '+', and '.'
                                  characters.
                                rule: 'self.find(''(@.*:)'') != '''' ? self.find(''(@.*:)'').matches(''(@[A-Za-z][A-Za-z0-9]*([_+.][A-Za-z][A-Za-z0-9]*)*[:])'')
                                  : true'
                              - message: digest must be at least 32 characters
                                rule: 'self.find(''(@.*:)'') != '''' ? self.find('':.*$'').substring(1).size()
                                  >= 32 : true'
                              - message: digest must only contain hex characters (A-F,
                                  a-f, 0-9)
                                rule: 'self.find(''(@.*:)'') != '''' ? self.find('':.*$'').matches('':[0-9A-Fa-f]*$'')
                                  : true'
                            paths:
                              description: |-
                                paths restricts which directories from the image are mounted.
                                Each path is mounted as a separate subPath volumeMount into the agent's
                                skills directory. The last segment of each path becomes the mount name
                                (e.g., "/skills/prometheus" mounts as "prometheus").

                                Each path must be an absolute file path: starts with "/", no ".."
                                or "." segments, no double slashes, no trailing slash, and only
                                alphanumeric characters, hyphens, underscores, dots, and slashes.

                                When omitted, the entire image is mounted as a single volume.
                                Maximum 50 items.
                              items:
                                maxLength: 512
                                minLength: 2
                                type: string
                              maxItems: 50
                              minItems: 1
                              type: array
                              x-kubernetes-list-type: atomic
                              x-kubernetes-validations:
                              - message: each path must be 2-512 characters
                                rule: self.all(p, p.size() >= 2 && p.size() <= 512)
                              - message: each path must be absolute (start with '/')
                                rule: self.all(p, p.startsWith('/'))
                              - message: paths must not end with '/'
                                rule: self.all(p, !p.endsWith('/'))
                              - message: paths must not contain double slashes
                                rule: self.all(p, !p.contains('//'))
                              - message: paths must not contain '.' or '..' segments
                                rule: self.all(p, !p.contains('/../') && !p.endsWith('/..')
                                  && !p.contains('/./') && !p.endsWith('/.'))
                              - message: paths may only contain alphanumeric characters,
                                  '/', '_', '.', and '-'
                                rule: self.all(p, p.matches('^[a-zA-Z0-9/_.-]+$'))
                          required:
                          - image
                          type: object
                        maxItems: 20
                        minItems: 1
                        type: array
                        x-kubernetes-list-map-keys:
                        - image
                        x-kubernetes-list-type: map
                    type: object
                type: object
                x-kubernetes-validations:
                - message: verification is immutable after creation
                  rule: self == oldSelf
            required:
            - analysis
            - request
            type: object
            x-kubernetes-validations:
            - message: analysis must be provided
              rule: has(self.analysis)
          status:
            description: status defines the observed state of Proposal.
            properties:
              attempts:
                description: |-
                  attempts is the number of times this proposal has been attempted
                  (1-based). Incremented each time the proposal is retried after a
                  failure. Starts at 1 for the first attempt.
                format: int32
                type: integer
              conditions:
                description: |-
                  conditions represent the latest available observations using the
                  standard Kubernetes condition pattern. Condition types include:
                  Analyzed, Approved, Executed, Verified, and Escalated.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              previousAttempts:
                description: |-
                  previousAttempts contains the failure history from earlier attempts.
                  Each entry records which step failed and why, giving the analysis
                  agent on the next attempt context to avoid repeating the same mistake.
                  Maximum 20 items.
                items:
                  description: |-
                    PreviousAttempt captures the state of a failed attempt. When a proposal
                    fails and retries, the operator records the failure context here so that
                    the analysis agent on the next attempt can learn from previous failures.
                    If maxAttempts is reached, the full history of PreviousAttempts is
                    included in the escalation child proposal.
                  properties:
                    attempt:
                      description: attempt is the 1-based attempt number that failed.
                      format: int32
                      minimum: 1
                      type: integer
                    failedStep:
                      description: failedStep is which workflow step failed (analysis,
                        execution, or verification).
                      enum:
                      - Analysis
                      - Execution
                      - Verification
                      type: string
                    failureReason:
                      description: |-
                        failureReason is the error message or explanation from the failed step.
                        Omit when no failure has occurred; an empty string is treated the same
                        as omitted (no failure). Maximum 8192 characters.
                      maxLength: 8192
                      minLength: 1
                      type: string
                  required:
                  - attempt
                  type: object
                maxItems: 20
                minItems: 1
                type: array
                x-kubernetes-list-type: atomic
              steps:
                description: |-
                  steps contains the per-step observed state (analysis, execution,
                  verification). Each step independently tracks its timing, sandbox
                  info, and results.
                properties:
                  analysis:
                    description: analysis is the observed state of the analysis step.
                    properties:
                      completionTime:
                        description: completionTime is when the step completed.
                        format: date-time
                        type: string
                      components:
                        description: |-
                          components contains optional adapter-specific UI components that
                          apply to the analysis step as a whole.
                        items:
                          x-kubernetes-preserve-unknown-fields: true
                        maxItems: 20
                        minItems: 1
                        type: array
                        x-kubernetes-list-type: atomic
                      conditions:
                        description: conditions for this step.
                        items:
                          description: Condition contains details for one aspect of
                            the current state of this API Resource.
                          properties:
                            lastTransitionTime:
                              description: |-
                                lastTransitionTime is the last time the condition transitioned from one status to another.
                                This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                              format: date-time
                              type: string
                            message:
                              description: |-
                                message is a human readable message indicating details about the transition.
                                This may be an empty string.
                              maxLength: 32768
                              type: string
                            observedGeneration:
                              description: |-
                                observedGeneration represents the .metadata.generation that the condition was set based upon.
                                For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                with respect to the current state of the instance.
                              format: int64
                              minimum: 0
                              type: integer
                            reason:
                              description: |-
                                reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                Producers of specific condition types may define expected values and meanings for this field,
                                and whether the values are considered a guaranteed API.
                                The value should be a CamelCase string.
                                This field may not be empty.
                              maxLength: 1024
                              minLength: 1
                              pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                              type: string
                            status:
                              description: status of the condition, one of True, False,
                                Unknown.
                              enum:
                              - "True"
                              - "False"
                              - Unknown
                              type: string
                            type:
                              description: type of condition in CamelCase or in foo.example.com/CamelCase.
                              maxLength: 316
                              pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                              type: string
                          required:
                          - lastTransitionTime
                          - message
                          - reason
                          - status
                          - type
                          type: object
                        maxItems: 8
                        minItems: 1
                        type: array
                        x-kubernetes-list-map-keys:
                        - type
                        x-kubernetes-list-type: map
                      observedRevision:
                        description: |-
                          observedRevision is the revision number from spec.revision that this
                          analysis was produced for. When spec.revision > observedRevision,
                          the operator re-runs analysis with revision context.
                        format: int32
                        minimum: 0
                        type: integer
                      options:
                        description: |-
                          options contains one or more remediation options returned by the
                          analysis agent. Each option has its own diagnosis, plan,
                          verification strategy, and RBAC requirements.
                        items:
                          description: |-
                            RemediationOption represents a single remediation approach produced by
                            the analysis agent. The agent may return multiple options, each with
                            its own diagnosis, remediation plan, verification strategy, and RBAC
                            requirements. The user selects one option after analysis
                            (recorded in AnalysisStepStatus.selectedOption), and the operator uses
                            that option's RBAC and plan for the execution step.

                            The components field is an extensibility point for adapter-specific UI
                            data. For example, an ACS adapter might include violation details or
                            affected deployment information as components that the console plugin
                            renders with custom components.
                          properties:
                            components:
                              description: |-
                                components contains optional adapter-defined structured data for
                                custom console UI rendering. Each entry is a raw JSON object.
                                Maximum 20 items.
                              items:
                                x-kubernetes-preserve-unknown-fields: true
                              maxItems: 20
                              minItems: 1
                              type: array
                              x-kubernetes-list-type: atomic
                            diagnosis:
                              description: diagnosis contains the root cause analysis
                                specific to this option.
                              properties:
                                confidence:
                                  description: |-
                                    confidence is the agent's self-assessed confidence in its diagnosis.
                                    Higher confidence generally correlates with clearer symptoms and
                                    more deterministic root causes.
                                  enum:
                                  - Low
                                  - Medium
                                  - High
                                  type: string
                                rootCause:
                                  description: |-
                                    rootCause is a concise Markdown-formatted description of the identified
                                    root cause (e.g., "OOMKilled due to memory limit of 256Mi").
                                    Maximum 1024 characters.
                                  maxLength: 1024
                                  minLength: 1
                                  type: string
                                summary:
                                  description: |-
                                    summary is a Markdown-formatted diagnosis summary explaining the
                                    problem, its symptoms, and the agent's findings. Maximum 8192 characters.
                                  maxLength: 8192
                                  minLength: 1
                                  type: string
                              required:
                              - confidence
                              - rootCause
                              - summary
                              type: object
                            proposal:
                              description: proposal contains the remediation plan
                                for this option.
                              properties:
                                actions:
                                  description: |-
                                    actions is the ordered list of discrete actions the agent proposes.
                                    Maximum 50 items.
                                  items:
                                    description: |-
                                      ProposedAction describes a single discrete action the analysis agent
                                      recommends as part of its remediation plan. Actions are displayed to
                                      the user after analysis for review before approval.
                                    properties:
                                      description:
                                        description: |-
                                          description is a Markdown-formatted explanation of what this action
                                          will do (e.g., "Increase memory limit from 256Mi to 512Mi").
                                          Maximum 4096 characters.
                                        maxLength: 4096
                                        minLength: 1
                                        type: string
                                      type:
                                        description: |-
                                          type is the action category (e.g., "patch", "scale", "restart",
                                          "create", "delete", "rollout"). Free-form string to allow agents
                                          to express domain-specific action types. Must be 1-256 characters.
                                        maxLength: 256
                                        minLength: 1
                                        type: string
                                    required:
                                    - description
                                    - type
                                    type: object
                                  maxItems: 50
                                  minItems: 1
                                  type: array
                                  x-kubernetes-list-type: atomic
                                description:
                                  description: |-
                                    description is a Markdown-formatted summary of the overall remediation
                                    approach. Maximum 8192 characters.
                                  maxLength: 8192
                                  minLength: 1
                                  type: string
                                estimatedImpact:
                                  description: |-
                                    estimatedImpact is a Markdown-formatted description of the expected
                                    impact of the remediation on the system
                                    (e.g., "Brief pod restart, ~30s downtime"). Omit when impact is
                                    unknown; an empty string is treated the same as omitted.
                                    Maximum 1024 characters.
                                  maxLength: 1024
                                  minLength: 1
                                  type: string
                                reversible:
                                  description: |-
                                    reversible indicates whether the remediation can be rolled back
                                    if something goes wrong. See rollbackPlan for details.
                                    Must be one of: Reversible, Irreversible, Partial.
                                  enum:
                                  - Reversible
                                  - Irreversible
                                  - Partial
                                  type: string
                                risk:
                                  description: |-
                                    risk is the agent's assessment of how risky the remediation is.
                                    Critical-risk proposals typically require explicit human review.
                                  enum:
                                  - Low
                                  - Medium
                                  - High
                                  - Critical
                                  type: string
                                rollbackPlan:
                                  description: |-
                                    rollbackPlan describes how to undo the remediation if execution fails
                                    or causes unexpected issues. Only the execution step mutates cluster
                                    state, so rollback lives here alongside the actions it would undo.
                                  properties:
                                    command:
                                      description: |-
                                        command is the rollback command or steps to execute.
                                        Must be 1-4096 characters.
                                      maxLength: 4096
                                      minLength: 1
                                      type: string
                                    description:
                                      description: |-
                                        description is a Markdown-formatted explanation of the rollback strategy.
                                        Must be 1-4096 characters.
                                      maxLength: 4096
                                      minLength: 1
                                      type: string
                                  required:
                                  - command
                                  - description
                                  type: object
                              required:
                              - actions
                              - description
                              - risk
                              type: object
                            rbac:
                              description: |-
                                rbac contains the RBAC permissions the execution agent will need.
                                The operator's policy engine validates these before creating the
                                actual Kubernetes RBAC resources. Omitted for advisory-only options.
                              properties:
                                clusterScoped:
                                  description: |-
                                    clusterScoped are rules that will be applied via ClusterRole +
                                    ClusterRoleBinding. Used when the agent needs cross-namespace or
                                    non-namespaced resource access (e.g., reading nodes, CRDs).
                                    Maximum 50 items.
                                  items:
                                    description: |-
                                      RBACRule describes a single RBAC permission that the analysis agent
                                      requests for the execution step. The operator's policy engine validates
                                      these requests against a 6-layer defense model before creating the
                                      actual Role/ClusterRole bindings. Each rule must include a justification
                                      so that users and policy can audit why the permission is needed.
                                    properties:
                                      apiGroups:
                                        description: |-
                                          apiGroups are the API groups for this rule (e.g., "", "apps", "batch").
                                          Maximum 20 items.
                                        items:
                                          maxLength: 253
                                          minLength: 1
                                          type: string
                                        maxItems: 20
                                        minItems: 1
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      justification:
                                        description: |-
                                          justification is a Markdown-formatted explanation of why this
                                          permission is needed for the remediation
                                          (e.g., "Need to patch deployment to increase memory limit").
                                          Required for audit and policy enforcement. Maximum 1024 characters.
                                        maxLength: 1024
                                        minLength: 1
                                        type: string
                                      namespace:
                                        description: |-
                                          namespace is the target namespace for namespace-scoped rules.
                                          Must match one of the proposal's targetNamespaces. Ignored for
                                          cluster-scoped rules. Validation is deferred to the operator's
                                          policy engine at runtime. Must be a valid RFC 1123 DNS label.
                                        maxLength: 63
                                        minLength: 1
                                        type: string
                                        x-kubernetes-validations:
                                        - message: 'must be a valid DNS label: lowercase
                                            alphanumeric characters and hyphens, starting
                                            with an alphabetic character and ending
                                            with an alphanumeric character'
                                          rule: '!format.dns1123Label().validate(self).hasValue()'
                                      resourceNames:
                                        description: |-
                                          resourceNames restricts the rule to specific named resources.
                                          When empty, the rule applies to all resources of the given type.
                                          Maximum 50 items.
                                        items:
                                          maxLength: 253
                                          minLength: 1
                                          type: string
                                        maxItems: 50
                                        minItems: 1
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      resources:
                                        description: |-
                                          resources are the resource types (e.g., "pods", "deployments").
                                          Maximum 20 items.
                                        items:
                                          maxLength: 253
                                          minLength: 1
                                          type: string
                                        maxItems: 20
                                        minItems: 1
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      verbs:
                                        description: |-
                                          verbs are the allowed operations (e.g., "get", "patch", "delete").
                                          Maximum 10 items.
                                        items:
                                          maxLength: 63
                                          minLength: 1
                                          type: string
                                        maxItems: 10
                                        minItems: 1
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - apiGroups
                                    - justification
                                    - resources
                                    - verbs
                                    type: object
                                  maxItems: 50
                                  minItems: 1
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceScoped:
                                  description: |-
                                    namespaceScoped are rules that will be applied via Role + RoleBinding
                                    in the proposal's target namespaces. These are the most common rules.
                                    Maximum 50 items.
                                  items:
                                    description: |-
                                      RBACRule describes a single RBAC permission that the analysis agent
                                      requests for the execution step. The operator's policy engine validates
                                      these requests against a 6-layer defense model before creating the
                                      actual Role/ClusterRole bindings. Each rule must include a justification
                                      so that users and policy can audit why the permission is needed.
                                    properties:
                                      apiGroups:
                                        description: |-
                                          apiGroups are the API groups for this rule (e.g., "", "apps", "batch").
                                          Maximum 20 items.
                                        items:
                                          maxLength: 253
                                          minLength: 1
                                          type: string
                                        maxItems: 20
                                        minItems: 1
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      justification:
                                        description: |-
                                          justification is a Markdown-formatted explanation of why this
                                          permission is needed for the remediation
                                          (e.g., "Need to patch deployment to increase memory limit").
                                          Required for audit and policy enforcement. Maximum 1024 characters.
                                        maxLength: 1024
                                        minLength: 1
                                        type: string
                                      namespace:
                                        description: |-
                                          namespace is the target namespace for namespace-scoped rules.
                                          Must match one of the proposal's targetNamespaces. Ignored for
                                          cluster-scoped rules. Validation is deferred to the operator's
                                          policy engine at runtime. Must be a valid RFC 1123 DNS label.
                                        maxLength: 63
                                        minLength: 1
                                        type: string
                                        x-kubernetes-validations:
                                        - message: 'must be a valid DNS label: lowercase
                                            alphanumeric characters and hyphens, starting
                                            with an alphabetic character and ending
                                            with an alphanumeric character'
                                          rule: '!format.dns1123Label().validate(self).hasValue()'
                                      resourceNames:
                                        description: |-
                                          resourceNames restricts the rule to specific named resources.
                                          When empty, the rule applies to all resources of the given type.
                                          Maximum 50 items.
                                        items:
                                          maxLength: 253
                                          minLength: 1
                                          type: string
                                        maxItems: 50
                                        minItems: 1
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      resources:
                                        description: |-
                                          resources are the resource types (e.g., "pods", "deployments").
                                          Maximum 20 items.
                                        items:
                                          maxLength: 253
                                          minLength: 1
                                          type: string
                                        maxItems: 20
                                        minItems: 1
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      verbs:
                                        description: |-
                                          verbs are the allowed operations (e.g., "get", "patch", "delete").
                                          Maximum 10 items.
                                        items:
                                          maxLength: 63
                                          minLength: 1
                                          type: string
                                        maxItems: 10
                                        minItems: 1
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - apiGroups
                                    - justification
                                    - resources
                                    - verbs
                                    type: object
                                  maxItems: 50
                                  minItems: 1
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                            summary:
                              description: |-
                                summary is an optional Markdown-formatted one-line summary for
                                collapsed views in the console UI. Maximum 1024 characters.
                              maxLength: 1024
                              minLength: 1
                              type: string
                            title:
                              description: |-
                                title is a short Markdown-formatted name for this option
                                (e.g., "Increase memory limit", "Restart with backoff").
                                Must be 1-256 characters.
                              maxLength: 256
                              minLength: 1
                              type: string
                            verification:
                              description: |-
                                verification contains the verification plan. Omitted when
                                verification is skipped in the workflow.
                              properties:
                                description:
                                  description: |-
                                    description is a Markdown-formatted summary of the verification approach.
                                    Maximum 4096 characters.
                                  maxLength: 4096
                                  minLength: 1
                                  type: string
                                steps:
                                  description: |-
                                    steps is the ordered list of verification checks to run.
                                    Maximum 20 items.
                                  items:
                                    description: |-
                                      VerificationStep describes a single verification check that the
                                      verification agent should run after execution. Populated by the
                                      analysis agent as part of the RemediationOption.
                                    properties:
                                      command:
                                        description: |-
                                          command is the command or API call to run for this check
                                          (e.g., "oc get pod -n production -l app=web -o jsonpath='{.items[0].status.phase}'").
                                          Must be 1-4096 characters.
                                        maxLength: 4096
                                        minLength: 1
                                        type: string
                                      expected:
                                        description: |-
                                          expected is the expected output or condition
                                          (e.g., "Running", "ready=true"). Maximum 1024 characters.
                                        maxLength: 1024
                                        minLength: 1
                                        type: string
                                      name:
                                        description: |-
                                          name is a short identifier for this check (e.g., "pod-running").
                                          Must be 1-253 characters.
                                        maxLength: 253
                                        minLength: 1
                                        type: string
                                      type:
                                        description: |-
                                          type categorizes the check (e.g., "command", "metric", "condition").
                                          Must be 1-256 characters.
                                        maxLength: 256
                                        minLength: 1
                                        type: string
                                    required:
                                    - command
                                    - expected
                                    - name
                                    - type
                                    type: object
                                  maxItems: 20
                                  minItems: 1
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - description
                              type: object
                          required:
                          - diagnosis
                          - proposal
                          - title
                          type: object
                        maxItems: 10
                        minItems: 1
                        type: array
                        x-kubernetes-list-type: atomic
                      sandbox:
                        description: sandbox tracks the sandbox used.
                        properties:
                          claimName:
                            description: |-
                              claimName is the name of the SandboxClaim resource that owns the
                              sandbox pod. Omit when no sandbox has been claimed; an empty string
                              is treated the same as omitted. Maximum 253 characters.
                            maxLength: 253
                            minLength: 1
                            type: string
                          completionTime:
                            description: completionTime is when the sandbox pod finished
                              (success or failure).
                            format: date-time
                            type: string
                          namespace:
                            description: |-
                              namespace is the namespace where the SandboxClaim and its pod live.
                              Must be a valid RFC 1123 DNS label.
                            maxLength: 63
                            minLength: 1
                            type: string
                            x-kubernetes-validations:
                            - message: 'must be a valid DNS label: lowercase alphanumeric
                                characters and hyphens, starting with an alphabetic
                                character and ending with an alphanumeric character'
                              rule: '!format.dns1123Label().validate(self).hasValue()'
                          startTime:
                            description: startTime is when the sandbox pod was created.
                            format: date-time
                            type: string
                        type: object
                      selectedOption:
                        description: |-
                          selectedOption is the 0-based index into the options array
                          that the user approved. Set when the user approves the proposal.
                          The operator uses this to determine which option's RBAC and plan
                          to use for execution. Minimum value: 0.
                        format: int32
                        minimum: 0
                        type: integer
                      startTime:
                        description: startTime is when the step started.
                        format: date-time
                        type: string
                    type: object
                  execution:
                    description: execution is the observed state of the execution
                      step.
                    properties:
                      actionsTaken:
                        description: actionsTaken lists what the agent did.
                        items:
                          description: |-
                            ExecutionAction describes a single action taken by the execution agent
                            during the execution step. These are recorded in ExecutionStepStatus
                            to provide an audit trail of what the agent actually did.
                          properties:
                            description:
                              description: |-
                                description is a Markdown-formatted explanation of what the agent did
                                (e.g., "Patched deployment/web to set memory limit to 512Mi").
                                Maximum 4096 characters.
                              maxLength: 4096
                              minLength: 1
                              type: string
                            error:
                              description: |-
                                error is the error message if the action failed.
                                Maximum 8192 characters.
                              maxLength: 8192
                              minLength: 1
                              type: string
                            outcome:
                              description: |-
                                outcome indicates whether this individual action succeeded.
                                Must be one of: Succeeded, Failed.
                              enum:
                              - Succeeded
                              - Failed
                              type: string
                            output:
                              description: |-
                                output is the command output or API response from the action.
                                Maximum 32768 characters.
                              maxLength: 32768
                              minLength: 1
                              type: string
                            type:
                              description: |-
                                type is the action category (e.g., "patch", "scale", "restart").
                                Maximum 256 characters.
                              maxLength: 256
                              minLength: 1
                              type: string
                          required:
                          - description
                          - type
                          type: object
                        maxItems: 100
                        minItems: 1
                        type: array
                        x-kubernetes-list-type: atomic
                      completionTime:
                        description: completionTime is when the step completed.
                        format: date-time
                        type: string
                      components:
                        description: components contains optional adapter-defined
                          structured data.
                        items:
                          x-kubernetes-preserve-unknown-fields: true
                        maxItems: 20
                        minItems: 1
                        type: array
                        x-kubernetes-list-type: atomic
                      conditions:
                        description: conditions for this step.
                        items:
                          description: Condition contains details for one aspect of
                            the current state of this API Resource.
                          properties:
                            lastTransitionTime:
                              description: |-
                                lastTransitionTime is the last time the condition transitioned from one status to another.
                                This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                              format: date-time
                              type: string
                            message:
                              description: |-
                                message is a human readable message indicating details about the transition.
                                This may be an empty string.
                              maxLength: 32768
                              type: string
                            observedGeneration:
                              description: |-
                                observedGeneration represents the .metadata.generation that the condition was set based upon.
                                For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                with respect to the current state of the instance.
                              format: int64
                              minimum: 0
                              type: integer
                            reason:
                              description: |-
                                reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                Producers of specific condition types may define expected values and meanings for this field,
                                and whether the values are considered a guaranteed API.
                                The value should be a CamelCase string.
                                This field may not be empty.
                              maxLength: 1024
                              minLength: 1
                              pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                              type: string
                            status:
                              description: status of the condition, one of True, False,
                                Unknown.
                              enum:
                              - "True"
                              - "False"
                              - Unknown
                              type: string
                            type:
                              description: type of condition in CamelCase or in foo.example.com/CamelCase.
                              maxLength: 316
                              pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                              type: string
                          required:
                          - lastTransitionTime
                          - message
                          - reason
                          - status
                          - type
                          type: object
                        maxItems: 8
                        minItems: 1
                        type: array
                        x-kubernetes-list-map-keys:
                        - type
                        x-kubernetes-list-type: map
                      retryCount:
                        description: |-
                          retryCount tracks how many times execution+verification has been
                          retried for the current analysis option. Reset when a new analysis
                          is run (initial or revision). The operator increments this on each
                          objective verification failure before retrying execution.
                        format: int32
                        minimum: 0
                        type: integer
                      sandbox:
                        description: sandbox tracks the sandbox used.
                        properties:
                          claimName:
                            description: |-
                              claimName is the name of the SandboxClaim resource that owns the
                              sandbox pod. Omit when no sandbox has been claimed; an empty string
                              is treated the same as omitted. Maximum 253 characters.
                            maxLength: 253
                            minLength: 1
                            type: string
                          completionTime:
                            description: completionTime is when the sandbox pod finished
                              (success or failure).
                            format: date-time
                            type: string
                          namespace:
                            description: |-
                              namespace is the namespace where the SandboxClaim and its pod live.
                              Must be a valid RFC 1123 DNS label.
                            maxLength: 63
                            minLength: 1
                            type: string
                            x-kubernetes-validations:
                            - message: 'must be a valid DNS label: lowercase alphanumeric
                                characters and hyphens, starting with an alphabetic
                                character and ending with an alphanumeric character'
                              rule: '!format.dns1123Label().validate(self).hasValue()'
                          startTime:
                            description: startTime is when the sandbox pod was created.
                            format: date-time
                            type: string
                        type: object
                      startTime:
                        description: startTime is when the step started.
                        format: date-time
                        type: string
                      success:
                        description: success indicates whether the execution agent
                          reported overall success.
                        type: boolean
                      verification:
                        description: |-
                          verification is the lightweight inline verification the execution
                          agent performs immediately after completing its actions.
                        properties:
                          conditionOutcome:
                            description: |-
                              conditionOutcome indicates whether the target condition improved
                              after the remediation (e.g., pod is no longer CrashLoopBackOff).
                              Must be one of: Improved, Unchanged, Degraded.
                            enum:
                            - Improved
                            - Unchanged
                            - Degraded
                            type: string
                          summary:
                            description: |-
                              summary is a Markdown-formatted summary of the inline verification.
                              Maximum 4096 characters.
                            maxLength: 4096
                            minLength: 1
                            type: string
                        required:
                        - summary
                        type: object
                    type: object
                  verification:
                    description: verification is the observed state of the verification
                      step.
                    properties:
                      checks:
                        description: checks contains individual verification check
                          results.
                        items:
                          description: |-
                            VerifyCheck is a single verification check result from the verification
                            agent. Each check corresponds to a VerificationStep from the analysis
                            agent's verification plan.
                          properties:
                            name:
                              description: |-
                                name is the check identifier, matching the VerificationStep name.
                                Maximum 253 characters.
                              maxLength: 253
                              minLength: 1
                              type: string
                            result:
                              description: |-
                                result indicates whether the check's observed value matches
                                the expected value. Must be one of: Passed, Failed.
                              enum:
                              - Passed
                              - Failed
                              type: string
                            source:
                              description: |-
                                source is what performed the check (e.g., "oc", "promql", "curl").
                                Maximum 256 characters.
                              maxLength: 256
                              minLength: 1
                              type: string
                            value:
                              description: |-
                                value is the actual observed value (e.g., "Running", "3 replicas").
                                Maximum 4096 characters.
                              maxLength: 4096
                              minLength: 1
                              type: string
                          required:
                          - name
                          - source
                          - value
                          type: object
                        maxItems: 50
                        minItems: 1
                        type: array
                        x-kubernetes-list-type: atomic
                      completionTime:
                        description: completionTime is when the step completed.
                        format: date-time
                        type: string
                      components:
                        description: components contains optional adapter-defined
                          structured data.
                        items:
                          x-kubernetes-preserve-unknown-fields: true
                        maxItems: 20
                        minItems: 1
                        type: array
                        x-kubernetes-list-type: atomic
                      conditions:
                        description: conditions for this step.
                        items:
                          description: Condition contains details for one aspect of
                            the current state of this API Resource.
                          properties:
                            lastTransitionTime:
                              description: |-
                                lastTransitionTime is the last time the condition transitioned from one status to another.
                                This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                              format: date-time
                              type: string
                            message:
                              description: |-
                                message is a human readable message indicating details about the transition.
                                This may be an empty string.
                              maxLength: 32768
                              type: string
                            observedGeneration:
                              description: |-
                                observedGeneration represents the .metadata.generation that the condition was set based upon.
                                For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                with respect to the current state of the instance.
                              format: int64
                              minimum: 0
                              type: integer
                            reason:
                              description: |-
                                reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                Producers of specific condition types may define expected values and meanings for this field,
                                and whether the values are considered a guaranteed API.
                                The value should be a CamelCase string.
                                This field may not be empty.
                              maxLength: 1024
                              minLength: 1
                              pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                              type: string
                            status:
                              description: status of the condition, one of True, False,
                                Unknown.
                              enum:
                              - "True"
                              - "False"
                              - Unknown
                              type: string
                            type:
                              description: type of condition in CamelCase or in foo.example.com/CamelCase.
                              maxLength: 316
                              pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                              type: string
                          required:
                          - lastTransitionTime
                          - message
                          - reason
                          - status
                          - type
                          type: object
                        maxItems: 8
                        minItems: 1
                        type: array
                        x-kubernetes-list-map-keys:
                        - type
                        x-kubernetes-list-type: map
                      sandbox:
                        description: sandbox tracks the sandbox used.
                        properties:
                          claimName:
                            description: |-
                              claimName is the name of the SandboxClaim resource that owns the
                              sandbox pod. Omit when no sandbox has been claimed; an empty string
                              is treated the same as omitted. Maximum 253 characters.
                            maxLength: 253
                            minLength: 1
                            type: string
                          completionTime:
                            description: completionTime is when the sandbox pod finished
                              (success or failure).
                            format: date-time
                            type: string
                          namespace:
                            description: |-
                              namespace is the namespace where the SandboxClaim and its pod live.
                              Must be a valid RFC 1123 DNS label.
                            maxLength: 63
                            minLength: 1
                            type: string
                            x-kubernetes-validations:
                            - message: 'must be a valid DNS label: lowercase alphanumeric
                                characters and hyphens, starting with an alphabetic
                                character and ending with an alphanumeric character'
                              rule: '!format.dns1123Label().validate(self).hasValue()'
                          startTime:
                            description: startTime is when the sandbox pod was created.
                            format: date-time
                            type: string
                        type: object
                      startTime:
                        description: startTime is when the step started.
                        format: date-time
                        type: string
                      success:
                        description: success indicates whether the verification agent
                          reported overall success.
                        type: boolean
                      summary:
                        description: summary is a Markdown-formatted verification
                          summary.
                        maxLength: 32768
                        minLength: 1
                        type: string
                    type: object
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
